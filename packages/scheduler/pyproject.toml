[project]
name = "dataforge-scheduler"
version = "1.0.0"
description = "Data-Forge scheduler runtime package"
requires-python = ">=3.11"
dependencies = [
    "dataforge-shared",
    "dataforge-worker-manager",
]

[tool.uv.sources]
dataforge-shared = { path = "../shared", editable = true }
dataforge-worker-manager = { path = "../worker-manager", editable = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
only-include = ["main.py", "scheduler_service.py"]
